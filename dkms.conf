# ==============================================================================
# DKMS INFRASTRUCTURE ENGINE: CUSTOM MULTI-MODULE CONFIGURATION
# REPO TARGET FOOTPRINT: https://github.com
# ==============================================================================

PACKAGE_NAME="rcraid"
PACKAGE_VERSION="1.0.0-custom"

# Point the build engine directly to your structured src/ layout tree
BUILT_MODULE_NAME[0]="rcraid"
BUILT_MODULE_LOCATION[0]=""
DEST_MODULE_LOCATION[0]="/updates/dkms/"

# Force the compiler to inherit the custom Makefile's subfolder tracking paths
MAKE[0]="make -C ${kernel_source_dir} M=${dkms_tree}/${PACKAGE_NAME}/${PACKAGE_VERSION}/build"
CLEAN="make -C ${kernel_source_dir} M=${dkms_tree}/${PACKAGE_NAME}/${PACKAGE_VERSION}/build clean"

AUTOINSTALL="yes"
REMAKE_INITRD="yes"
