# ==============================================================================
# HARDENED DKMS PROFILE ENGINE: MULTI-FILE ARCHITECTURE FOR RESTRUCTURED TREE
# TARGET REPOSITORY PATH: /home/chazz/amd-raid-driver
# ==============================================================================

PACKAGE_NAME="rcraid"
PACKAGE_VERSION="1.0.0-custom"

# Core target kernel object name mapping
BUILT_MODULE_NAME[0]="rcraid"
DEST_MODULE_LOCATION[0]="/updates/dkms/"

# Inherit the master Makefile parameters directly
MAKE[0]="make -C ${kernel_source_dir} M=${dkms_tree}/${PACKAGE_NAME}/${PACKAGE_VERSION}/build"
CLEAN="make -C ${kernel_source_dir} M=${dkms_tree}/${PACKAGE_NAME}/${PACKAGE_VERSION}/build clean"

# Automate background image rebuild tasks upon successful compilation passes
AUTOINSTALL="yes"
REMAKE_INITRD="yes"
